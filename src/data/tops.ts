import type { Option } from "../types/option";
import { Slot } from "../types/slot";

export const TOP_OPTIONS: Option[] = [
    {
        name: "testItem",
        asset: "isabel_0008_tops-cools-club.png",
        slot: Slot.TOP,
        isLocked: true,
    }
]