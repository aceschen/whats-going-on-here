import type { Option } from "../types/option";
import { Slot } from "../types/slot";

export const BASE_OPTIONS: Option[] = [
    {
        name: "testItem",
        asset: "base_body.png",
        slot: Slot.BASE_BODY,
        isLocked: true,
    }
]