import type { Option } from "../types/option";
import { Slot } from "../types/slot";

export const PANTS_OPTIONS: Option[] = [
    {
        name: "testItem",
        asset: "1.png",
        slot: Slot.PANTS,
        isLocked: true,
    }
]